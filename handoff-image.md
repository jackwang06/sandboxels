# Image2 文明建筑与武器贴图生成交接文档

## 任务目标

为 Sandboxels 的文明系统生成一套像素风建筑贴图。游戏共有六个时代：部落时代、石器时代、农耕时代、青铜时代、铁器时代、城堡时代。

同一种建筑进入不同时代后，必须像经过真正的重建和升级，而不是仅仅更换颜色。相邻时代的版本至少要同时改变三项：整体轮廓、屋顶形式、建筑高度、主要材料、门窗结构、标志性设施。

第一批只生成完工建筑的正常状态。不要在第一批中生成损坏、燃烧、施工或废墟版本。

## 游戏内硬约束

- 每栋建筑在游戏逻辑中只有一个 `1x1` 核心，视觉贴图可以是横向、纵向或接近方形，不要强行拉伸成 `3x3` 正方形。
- 图像锚点是贴图非透明有效区域的底部中心。底部中央必须存在清楚、稳定的承重点，并能容纳一个逻辑核心方格，不能让主体悬空或向一侧倾倒。
- 游戏会裁切透明留白、保持贴图原始长宽比，并允许玩家在设置中将贴图缩放为 `50%` 至 `200%`；因此不要用透明留白伪造尺寸。
- 建筑绘制在地形、矿物、液体和落地资源上方，但植物、动物和人类会显示在建筑上方，因此轮廓之外必须完全透明。
- 每张图只包含一栋建筑，不要出现人物、动物、树木、地面、天空或完整场景。
- 建筑可以有木堆、石堆、农作物等附属物，但必须与主体构成一个连续、易识别的整体，不能形成第二栋建筑。
- 阵营颜色不能覆盖整栋建筑。主体使用时代材料色，只在旗帜、布篷、盾牌、门楣或窄边饰上使用阵营色区域。

## 输出规格

- 每次只生成一张独立建筑图，不要生成 sprite sheet，不要把多个方案拼在一张图上。
- 输出透明背景 PNG，母版尺寸 `1024x1024`。
- 构图必须适合之后使用 nearest-neighbor 缩小到 `64x64` 运行时画布，缩放时保持原始长宽比。
- 非透明有效区域应充分利用画布，但四周保留少量透明安全边距。不要为了填满正方形而改变建筑比例。
- 逻辑核心位于非透明有效区域底部中央；底部中央承重点必须落在画布有效范围内。
- 使用硬边像素块、清楚的色阶和成组像素，不要使用平滑笔刷、模糊边缘或照片纹理。
- 光源统一来自左上方。每种材料使用约 3 至 5 个明暗色阶。
- 缩小到 `64x64` 后，门、屋顶、烟囱、塔楼等识别特征仍应清楚。
- 文件名全部使用小写英文：`building_<era_id>_<building_id>.png`。

## 阵营换色规范

阵营色区域使用单一占位色 `#ff00ff`，建议占可见面积的 10% 至 20%。这个颜色只允许出现在旗帜、布篷、盾徽底板、窄边饰等可染色部位，不能出现在阴影、砖石、木材和金属中。占位色区域内部不要绘制文字或复杂图案，便于程序运行时统一换色。

## 所有提示词共用的基础段

将下面这段放在每条生成提示词的开头：

```text
为一款横向二维沙盒游戏生成一栋独立的像素风建筑贴图。透明背景 PNG，单体建筑，正面或接近正面的正交侧视图，只允许轻微展示屋顶顶面，不使用等距视角。输出 1024x1024 母版，并确保可以使用 nearest-neighbor 保持比例地清晰缩小到 64x64 运行时画布。建筑可以是横向、纵向或接近方形，禁止强行拉伸成正方形。建筑唯一的 1x1 逻辑锚点位于非透明有效区域的底部中心，底部中央必须有稳定承重点并完整落在有效范围内。有效轮廓充分利用画布，只保留少量透明安全边距。使用清晰、成组的方形像素块，硬边缘，每种材料 3 至 5 个色阶，左上方光源，强轮廓，缩小后仍容易识别。主体采用自然材料色；只在旗帜、布篷、盾徽底板或窄边饰上使用纯 #ff00ff 作为阵营换色占位色，占可见面积 10% 至 20%，图中其他位置禁止使用该颜色。一张图只画一栋建筑。
```

## 通用负面提示词

将下面这段放在每条生成提示词的末尾：

```text
不要背景，不要天空，不要地面贴片，不要完整场景，不要人物，不要动物，不要植物遮挡主体，不要第二栋建筑，不要文字，不要数字，不要 UI，不要边框，不要 logo，不要水印，不要照片写实，不要 3D 渲染，不要矢量插画，不要等距视角，不要鱼眼透视，不要柔焦，不要景深，不要辉光，不要半透明烟雾，不要抗锯齿，不要平滑渐变，不要细碎噪点，不要用大面积透明留白伪造小贴图，不要把长方形建筑拉伸成正方形，不要让附属物越出画布。
```

## 六个时代的视觉语言

每次生成时，在基础段后加入对应时代段，再加入建筑类型段。

### 1. 部落时代 `tribal`

```text
时代风格为部落时代。建筑低矮、不对称、手工感强，使用未经加工的圆木、树枝、藤条、芦苇、兽皮和少量骨制连接件。采用圆顶、斜靠式或粗糙茅草屋顶，支柱略微歪斜，结构简单但可用。轮廓中要有绳结、木桩或图腾式构件，绝不能出现砖、切割石块、金属屋顶、玻璃或规整城墙。整体轮廓应松散、原始、贴近临时营地。
```

### 2. 石器时代 `stone`

```text
时代风格为石器时代。建筑由粗切石块、干砌矮墙、巨石基座、厚木梁和石板构成，比部落时代更稳固、更方正。使用沉重石门楣、石板屋顶或厚茅草顶，强调宽而低的轮廓。保留少量粗木连接件，但不再使用大量兽皮。绝不能出现铜、铁、砖拱、精细雕刻或高耸城堡。与部落时代相比，必须明显增加石材体量并改变屋顶和入口轮廓。
```

### 3. 农耕时代 `agriculture`

```text
时代风格为农耕时代。建筑规划规整，使用木框架、夯土或浅色泥墙、编木抹泥墙、整齐茅草人字屋顶和低石基。轮廓强调定居村庄、粮食储存和农业生产，可出现整齐栅栏、晒架、陶罐、谷仓通风口，但不能出现复杂金属机械。与石器时代相比，建筑应更高、更整洁、更有明确门窗和屋脊；不要画成石头堡垒。
```

### 4. 青铜时代 `bronze`

```text
时代风格为青铜时代。使用规整泥砖、烧制砖、浅色切石基座、木梁和少量青铜包边。采用平顶、阶梯式屋顶、柱廊或厚重矩形门框，允许出现青铜铆钉、铜绿色氧化痕迹、太阳圆盘等无文字几何饰件。整体更城市化、更有行政秩序。与农耕时代相比，必须把茅草人字屋顶改为平顶或阶梯轮廓，并加入少量可辨认的青铜构件；不要画成希腊神庙或中世纪城堡。
```

### 5. 铁器时代 `iron`

```text
时代风格为铁器时代。使用深色粗木框架、坚固石基、灰色石墙、烧制瓦片和明显的黑铁加固件。采用较陡屋顶、铁箍门、铆接支架、烟囱、吊架或铁制工具轮廓，建筑比青铜时代更紧凑、更重、更有防御感。金属只作结构和工具，不要大面积覆盖墙体。与青铜时代相比，必须放弃平顶泥砖宫殿轮廓，改成高坡顶、深色铁件和强化石木结构；还不能出现完整的中世纪尖塔和华丽城堡。
```

### 6. 城堡时代 `castle`

```text
时代风格为城堡时代。使用规整切石墙、砖石拱门、扶壁、垛口、深色木梁、瓦片或石板高坡屋顶。建筑更高、更垂直、更精密，可出现塔楼、拱窗、吊桥机构、行会标志底板和大型烟囱。轮廓应体现成熟城镇与军事工程，但仍是紧凑、易识别的游戏建筑。与铁器时代相比，必须具有精砌石材、拱券、垛口或塔式纵向结构中的至少两项；不要出现蒸汽机、现代玻璃幕墙、电灯或现代机械。
```

## 建筑类型提示段

以下段落负责保证不同功能的建筑也能一眼区分。生成时选择一个段落加入提示词。

| `building_id` | 中文名 | 加入提示词的识别特征 |
| --- | --- | --- |
| `town_center` | 城镇中心 | 聚落中最高、最醒目的公共建筑；中央入口、集会标志和一面阵营色旗帜；轮廓庄重，不要像住宅。 |
| `hut` | 住房 | 紧凑的单户住宅；清楚的门、小窗和烟口；体量较小，不要出现生产设备。 |
| `hearth` | 火塘 | 低矮公共火塘或有遮棚的炉火；石圈、燃料架和排烟构件是重点；火焰只能很小，不能遮挡轮廓。 |
| `lumberyard` | 伐木场 | 开放式锯木棚、原木架、木料堆和显眼的锯台；木堆必须收在主体轮廓内。 |
| `workshop` | 工坊 | 半开放工作棚、结实工作台、工具架和宽门；不要画成熔炉或住宅。 |
| `quarry` | 采石场 | 切石棚、石料堆、简单吊架或滑轮；底部最厚重，具有明显凿开的方石。 |
| `gate` | 城门 | 中央必须是可通行门洞，两侧结构对称；石器至青铜版本偏木栅门，铁器以后偏石门和铁制门扇。 |
| `farm` | 农场 | 低矮农田、整齐田垄、小型工具棚或灌溉槽的组合；不能让植物遮住全部建筑轮廓。 |
| `granary` | 粮仓 | 架高粮仓、粗大储粮仓体、通风口或多个粮箱；入口较小，轮廓强调防潮储存。 |
| `market` | 市场 | 一至两个连成整体的摊棚、宽布篷、货架和陶罐；阵营色主要放在布篷，不能出现人物。 |
| `kiln` | 炭窑 | 封闭的圆顶或厚壁窑体、一个短烟囱、木炭入口；外形圆厚，不要像铁匠铺。 |
| `foundry` | 铸造厂 | 高温炉体、浇铸槽、坩埚轮廓和较高排烟道；比炭窑更工业化，但仍符合对应时代材料。 |
| `forge` | 锻造工坊 | 半开放锻炉、铁砧剪影、风箱和烟囱；黑铁工具清楚可见，不要画成大型铸造厂。 |
| `watchtower` | 瞭望塔 | 窄而高的塔身、顶部观察平台和遮棚；下部围护较少，轮廓不能像城堡主楼。 |
| `keep` | 城堡主楼 | 宽厚、封闭、极其坚固的主楼；中央塔体、垛口、拱门和小窗；只生成城堡时代版本。 |
| `siege_workshop` | 攻城工坊 | 大跨度开放工棚、绞盘、粗木梁和局部可见的攻城器械框架；不要生成一台独立投石机。 |
| `library` | 图书馆 | 对称的学术建筑、拱窗、书卷或石板形无文字徽记、安静封闭的入口；只生成城堡时代版本。 |

## 时代与建筑生成清单

早期解锁的建筑在后续时代继续拥有新版贴图。不要给尚未解锁的建筑生成历史倒置版本。

| 时代 | 需要生成的 `building_id` |
| --- | --- |
| `tribal` | `town_center`, `hut`, `hearth`, `lumberyard` |
| `stone` | `town_center`, `hut`, `hearth`, `lumberyard`, `workshop`, `quarry`, `gate` |
| `agriculture` | `town_center`, `hut`, `hearth`, `lumberyard`, `workshop`, `quarry`, `gate`, `farm`, `granary`, `market` |
| `bronze` | `town_center`, `hut`, `hearth`, `lumberyard`, `workshop`, `quarry`, `gate`, `farm`, `granary`, `market`, `kiln`, `foundry` |
| `iron` | `town_center`, `hut`, `hearth`, `lumberyard`, `workshop`, `quarry`, `gate`, `farm`, `granary`, `market`, `kiln`, `foundry`, `forge`, `watchtower` |
| `castle` | `town_center`, `hut`, `hearth`, `lumberyard`, `workshop`, `quarry`, `gate`, `farm`, `granary`, `market`, `kiln`, `foundry`, `forge`, `watchtower`, `keep`, `siege_workshop`, `library` |

共需生成 64 张正常状态建筑贴图。

## 提示词拼装格式

每张图的完整提示词按下列顺序组成：

```text
[所有提示词共用的基础段]

[对应时代的视觉语言]

建筑类型是“<中文名>”，内部 ID 为 <building_id>。<建筑类型表中的识别特征>

这张图必须与上一时代同类建筑在整体轮廓、屋顶、主要材料和标志性构件上明显不同，不能只做换色。文件名为 building_<era_id>_<building_id>.png。

[通用负面提示词]
```

## 六个城镇中心的直接生成提示词差异段

城镇中心是最能体现时代演进的建筑，六张图应优先生成并排检查。基础段和负面提示词仍需添加，这里只列中间的差异段。

### `building_tribal_town_center.png`

```text
部落时代城镇中心：一座不对称的部落议事长屋，弧形粗木骨架、芦苇和兽皮覆盖屋顶，中央有宽阔低矮入口，屋脊插有一面 #ff00ff 阵营旗和简朴木制图腾。整体低而宽，绳结与木桩清楚可见，没有石墙和金属件。
```

### `building_stone_town_center.png`

```text
石器时代城镇中心：一座建在巨石基座上的厚重议事厅，粗切石墙、巨大石门楣、厚木梁和石板屋顶，中央入口方正，顶部有小型 #ff00ff 旗帜。轮廓宽厚且接近矩形，与部落长屋的弧形兽皮轮廓完全不同。
```

### `building_agriculture_town_center.png`

```text
农耕时代城镇中心：一座规整的木框架村公所，浅色泥墙、高耸茅草人字屋顶、中央双开木门、两侧小窗和屋檐下的储粮标志，门楣悬挂 #ff00ff 布旗。轮廓更高、更对称，明显属于稳定农业村庄。
```

### `building_bronze_town_center.png`

```text
青铜时代城镇中心：一座城市化的泥砖行政厅，阶梯式平顶、浅色切石台基、中央厚重矩形门框、短柱廊和少量青铜包边，屋顶中央竖立 #ff00ff 方旗。整体呈水平分层和台阶轮廓，不保留农耕时代的茅草人字屋顶。
```

### `building_iron_town_center.png`

```text
铁器时代城镇中心：一座带防御感的石木议政厅，深色粗木框架、灰色石基、陡峭瓦顶、黑铁门箍和铁制支架，正面有较窄的高入口及 #ff00ff 盾徽底板。轮廓紧凑、屋顶高耸，与青铜时代的平顶台阶建筑明显不同。
```

### `building_castle_town_center.png`

```text
城堡时代城镇中心：一座精砌石材城镇大厅，中央拱门、两侧扶壁、高坡石板屋顶、短塔楼、拱窗和局部垛口，塔顶悬挂 #ff00ff 阵营旗。轮廓高而垂直，具有成熟中世纪市政建筑特征，但不能画成城堡主楼。
```

## Image2 自检要求

每完成一张图，按以下条件判断是否重做：

1. 背景是否真正透明，且只有一栋建筑。
2. 缩小到 `64x64` 后是否还能立刻认出建筑功能。
3. 建筑是否围绕非透明有效区域的底部中心锚点落地，而不是围绕画布最底边或左下角落地。
4. `#ff00ff` 是否只用于可换色部位，主体材料是否保持中性色。
5. 与前一时代的同类建筑相比，是否至少改变了三项结构特征。
6. 是否误用了后世材料，例如部落时代出现金属、青铜时代出现城堡垛口。
7. 是否出现文字、水印、人物、场景背景、过度烟雾或越界附属物。
8. 是否保持了建筑原始长宽比，并避免用过量透明留白导致游戏内有效贴图过小。

如果第 2、3、4、5 项中任意一项不满足，必须重新生成，不要依靠后期裁剪勉强修复。

# 第二部分：文明武器贴图

## 武器任务目标

为文明人生成一套具有 Minecraft 式低分辨率可读性、但轮廓与逐像素构成完全原创的武器贴图。第一版只生成单一正常持握状态，不生成挥砍动画、蓄力动画、损坏状态、掉落阴影或粒子效果。

当前武器系统共有七种状态：`fists`、`club`、`spear`、`bronze_spear`、`iron_sword`、`bow`、`crossbow`。`fists` 表示徒手，不生成贴图；实际需要交付 6 张武器贴图。

## 原创与参考边界

- 可以借鉴 Minecraft 物品贴图的视觉语言：低分辨率、清晰阶梯轮廓、少量色阶、强明暗分区、斜向持握构图和透明背景。
- 禁止直接复制、描摹或局部拼接 Minecraft 原版纹理。
- 禁止使用与 Minecraft 原贴图完全相同的逐像素轮廓、像素坐标、调色板和明暗排列。
- 木棍不能直接画成 Minecraft 木棍或木剑；必须成为末端更粗、轮廓不规则的战斗木棒。
- 长矛只能参考三叉戟的长柄可读性，必须改为单尖矛头，不能保留三叉结构。
- 不要出现 Minecraft、Mojang、苦力怕脸、附魔光效或其他品牌标志。

## 武器输出规格

- 每次只生成一件独立武器，不要生成 sprite sheet，不要把多个方案拼在一张图上。
- 输出透明背景 PNG，母版尺寸 `1024x1024`。
- 将母版理解为严格的 `16x16` 逻辑像素网格，每个逻辑像素都是边界清晰的同尺寸正方形色块。
- 构图必须能使用 nearest-neighbor 清晰缩小到 `16x16`，运行时可以再以 nearest-neighbor 放大到 `32x32`。
- 默认朝向为右：握持点位于画面左下区域，武器攻击端或尖端朝向右上。游戏运行时会水平翻转得到左朝向版本。
- 非透明有效区域应占画布宽度或高度的 75% 至 90%，避免大面积透明留白导致游戏内武器过小。
- 握持点周围必须保留 1 至 2 个逻辑像素的透明空间，方便贴到人物身体中心。
- 最细的剑刃、弓弦、矛杆仍至少占 1 个完整逻辑像素，不能使用亚像素线条。
- 使用硬边像素块，不抗锯齿，不模糊。每种材料使用 3 至 5 个色阶，统一采用左上方光源。
- 武器使用木材、石材、青铜和铁的自然材料色，不使用建筑阵营占位色 `#ff00ff`，武器不随阵营换色。
- 文件名使用：`weapon_club.png`、`weapon_spear.png`、`weapon_bronze_spear.png`、`weapon_iron_sword.png`、`weapon_bow.png`、`weapon_crossbow.png`。

## 武器共用基础提示词

将下面这段放在每条武器提示词开头：

```text
为一款横向二维沙盒游戏生成一件独立、原创的像素风武器物品贴图。视觉语言借鉴经典低分辨率方块沙盒游戏物品的清晰度：16x16 逻辑像素网格、硬边方形像素块、强轮廓、3 至 5 个明暗色阶、左上方光源、透明背景，但不得复制或描摹任何现有游戏的原版纹理、逐像素轮廓或调色板。输出 1024x1024 透明 PNG 母版，确保可以使用 nearest-neighbor 清晰缩小到 16x16。默认朝向右，握持点位于左下区域，武器的攻击端朝向右上；非透明有效区域占画布宽度或高度的 75% 至 90%。只画一件武器，不画持有者。
```

## 武器通用负面提示词

将下面这段放在每条武器提示词末尾：

```text
不要背景，不要地面，不要场景，不要人物，不要手臂，不要手套，不要第二件武器，不要多个角度，不要 sprite sheet，不要物品栏格子，不要 UI，不要文字，不要数字，不要 logo，不要水印，不要 Minecraft 或 Mojang 标志，不要苦力怕图案，不要附魔辉光，不要掉落阴影，不要粒子，不要火焰，不要血液，不要照片写实，不要 3D 渲染，不要等距模型，不要矢量插画，不要抗锯齿，不要平滑渐变，不要柔焦，不要半透明边缘，不要细碎噪点，不要复制现有游戏纹理。
```

## 六件武器的直接提示段

生成时使用“武器共用基础提示词 + 对应武器提示段 + 武器通用负面提示词”。

### `weapon_club.png`

```text
武器是部落时代的战斗木棒。使用深浅棕色原木，握柄较细，攻击端明显变粗并略微不规则，带有一圈藤条绑带或削切痕迹。整体是一根粗犷钝器，不是直而均匀的普通木棍，不是棒球棒，也不能画成木剑。轮廓从左下向右上延伸，粗端位于右上。
```

### `weapon_spear.png`

```text
武器是石器时代长矛。使用细长深棕木杆、单个灰色燧石三角矛头和浅棕绑绳。矛头必须只有一个尖端，石片轮廓略微不对称但十分锋利；杆身贯穿主要对角线，握持端在左下，矛头在右上。强调长柄武器轮廓，绝不能画成三叉戟、鱼叉、剑或箭。
```

### `weapon_bronze_spear.png`

```text
武器是青铜时代长矛。使用深色硬木杆、叶片形单尖青铜矛头、短套筒和绑带。青铜矛头采用暗棕橙、铜橙和一小块金黄色高光，可有极少量暗绿色氧化色，但不能像生锈铁器。轮廓比石矛更加对称、规整和精工，握持端在左下，矛头在右上；不能画成三叉戟。
```

### `weapon_iron_sword.png`

```text
武器是铁器时代单手铁剑。使用原创的笔直银灰剑刃、较深的刃脊阴影、短而清楚的深灰护手、棕色缠绕握柄和小型铁质剑首。剑刃从左下向右上延伸，长度明显大于握柄，轮廓简洁有力。可以借鉴经典像素剑的斜向可读性，但护手比例、剑尖形状、明暗分区和像素排列必须原创，不能逐像素复刻 Minecraft 铁剑。
```

### `weapon_bow.png`

```text
武器是农耕时代木弓。使用深棕与蜂蜜棕色弓臂、浅灰或米白色单像素弓弦，弓身具有清楚的弧形阶梯轮廓和中央握把。默认是未拉弦的空弓，不包含箭，不显示手。整体斜向放置并保留明显负空间，不能画成半圆图标、弩、乐器或与 Minecraft 原版弓完全相同的像素排列。
```

### `weapon_crossbow.png`

```text
武器是城堡时代机械弩。使用深棕木制托身、较浅木质横弓臂、灰铁机括、浅灰弓弦和短小的上方导轨。横弓臂、中央机括和长托身必须一眼可辨，整体比弓更厚重、更机械化。默认处于已上弦但不含弩箭的状态，不显示手，不生成飞行弹丸；不能画成步枪、普通弓或与 Minecraft 原版弩相同的像素排列。
```

## 武器自检要求

每完成一张武器图，按以下条件判断是否重做：

1. 背景是否真正透明，并且只有一件武器。
2. 缩小到 `16x16` 后是否仍能立即分辨武器类型。
3. 是否保持右朝向、左下握持点和右上攻击端。
4. 非透明有效区域是否足够大，是否因透明留白过多而显得很小。
5. 石矛和青铜矛是否都是单尖，且材质差异明显。
6. 木棒是否明显是钝器，而不是普通木棍或木剑。
7. 弓与弩是否能仅凭轮廓区分，且没有把箭或弹丸画进贴图。
8. 是否出现抗锯齿、半透明边缘、平滑渐变、文字、水印或品牌标志。
9. 是否只是逐像素复刻 Minecraft 原版贴图；如果是，必须重新设计轮廓与调色板。

如果第 1、2、4、5、7、9 项中任意一项不满足，必须重新生成。
